# WebSocket URL for online versus (friends on different networks).
# Deploy server/index.mjs to Render/Railway/Fly.io, then set:
# VITE_ONLINE_WS_URL=wss://your-relay-service.onrender.com
#
# Local dev (npm run dev) sets this automatically.

VITE_ONLINE_WS_URL=ws://localhost:8787
